{
  "name": "realm-of-ages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && ./node_modules/.bin/vite build --outDir ../public && cd ../server && npm install",
    "start": "node server/index.js"
  }
}